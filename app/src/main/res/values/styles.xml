<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="EcoDrivingSwitcher" parent="AppTheme">
        <!-- active thumb & track color -->
        <item name="colorControlActivated">@color/History.Calendar.Switch.BaseColor</item>
        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/History.Calendar.Switch.BaseColor</item>
        <!-- inactive track color -->
        <item name="android:colorForeground">@color/History.Calendar.Switch.BaseColor</item>
    </style>

</resources>
